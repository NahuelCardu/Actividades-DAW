// Ejercicio de manipulación de arrays
let numeros = [1, 2, 3, 4, 5];
let sumas = 0;
for (let i = 0; i < numeros.length; i++) {
    sumas += numeros[i];
}
console.log("La suma de los números es:", sumas);
